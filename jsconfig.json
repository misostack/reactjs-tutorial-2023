{
    "compilerOptions": {
      "typeRoots": ["node_modules/vitest/dist/index"],
      "baseUrl": ".",
      "target": "ES2020",
      "jsx": "react-jsx",
      // … all other compiler options
  
      // all paths defined here must match the configured path in `vite.config.ts`
      "paths": {
        "@app/*": [
          "./src/*"
        ]
      }
    },
    "include": ["node_modules/vitest", "node_modules/@testing-library"],
    "exclude": [],
}